//! Realtime sync test suite — Wave-0 scaffold for Phase 6.
//!
//! Run: `cargo test --features ssr --test realtime`
//!
//! broadcast.rs — BoardRoomRegistry fan-out + isolation tests (Task 2)
//! notif.rs     — UserNotifRegistry tests (placeholders for 06-05)
//! presence.rs  — PresenceRegistry tests (placeholders for 06-04)

mod broadcast;
mod notif;
mod presence;
